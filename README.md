GUI autoclicker based on AHK v2. Supports both hold and toggle modes.

Not as robust as other autoclickers, but requires no installation and works on a FOSS scripting language.

Default hotkey is Right Shift, but you can change it to whatever you want.

I should probably add a button to change the hotkey more easily, but I'm too lazy.
